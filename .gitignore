# Godot 4+ specific ignores
.godot/
.import/
export.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# System/tool-specific ignores
.DS_Store
*.swp
*.swo
*~
*.tmp

# Build artifacts (exclude most, but keep web build)
builds/
*.x86_64
*.exe
*.zip

# Exclude the downloaded Godot executable
Godot_v4.*
godot.zip

# Keep web build files
!build/web/*.pck
!build/web/*.wasm
!build/web/*.js
!build/web/*.html
!build/web/*.png
!export_presets.cfg
